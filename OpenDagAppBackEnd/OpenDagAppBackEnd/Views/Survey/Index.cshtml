@model IEnumerable<OpenDagAppBackEnd.Models.Survey>

@{
    ViewBag.Title = "Index";
}

<div class="bodyForms">
    <h2>Overzicht Studiewijzers</h2>

    <p>
        @Html.ActionLink("Creëer nieuw", "Create")
    </p>
    <table class="table" border="1">
        <tr>
            <th>
                Naam
                @*@Html.DisplayNameFor(model => model.Name)*@
            </th>
            <th>
                Actief
                @*@Html.DisplayNameFor(model => model.Active)*@
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <center>@Html.DisplayFor(modelItem => item.Active)</center>
                </td>
                <td>
                    @Html.ActionLink("Aanpassen", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }

    </table>
</div>